import UploadModal from '@/components/UploadModal';

// Renders the user dashboard with the upload entry point.
export default function DashboardPage() {
  return (
    <main>
      <h1>Docsflip Dashboard</h1>
      <UploadModal />
    </main>
  );
}
