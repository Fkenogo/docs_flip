// Placeholder callable for premium offline zip generation.
const functions = require('firebase-functions/v1');

exports.generateOfflineZip = functions.https.onCall(async () => {
  return { message: 'Not implemented in Priority 1' };
});
