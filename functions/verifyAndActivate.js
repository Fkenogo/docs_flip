// Placeholder callable for payment verification and tier activation.
const functions = require('firebase-functions/v1');

exports.verifyAndActivate = functions.https.onCall(async () => {
  return { message: 'Not implemented in Priority 1' };
});
